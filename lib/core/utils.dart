import 'package:flutter/material.dart';

const double defaultPadding = 8.0;
const double defaultRadius = 12.0;
const Duration defaultDuration = Duration(milliseconds: 800);
const Color black = Color(0xFF00010C);
const Color lightGrey = Color(0xFF636682);

const String baseUrl = "https://rickandmortyapi.com/api";
